<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Human-readable console output for local development -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- JSON output for production / log aggregators (Splunk, ELK, Datadog) -->
  <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
    </encoder>
  </appender>

  <!--
    To switch to JSON output, change the appender-ref below to JSON_CONSOLE.
    In a Spring Boot app you can use <springProfile name="prod"> blocks instead.
  -->
  <root level="DEBUG">
    <appender-ref ref="CONSOLE"/>
  </root>

</configuration>
